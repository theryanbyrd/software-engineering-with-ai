# tools/ — GitHub Actions, editor configs

Reusable tooling for teams adopting the harness.

| Path | Purpose |
|---|---|
| [`github-actions/`](github-actions/) | Reusable workflows (audit, reading-list freshness, prompt-injection CI) |
| `editor-configs/` *(forthcoming)* | VS Code / JetBrains settings for the harness |

Status: `github-actions/` is wired up as of `v2026.q3` (see [`.github/workflows/`](../.github/workflows/)). `editor-configs/` is forthcoming.
